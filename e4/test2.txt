There are two types of tasks: map tasks, which generate a set
of key-value pairs from the input file and write them to intermediate files,
and reduce tasks, which generate the final files with the processed and alphabetically sorted information.